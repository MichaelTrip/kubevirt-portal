# Environment Configuration for KubeVirt Portal

# ============================================
# REQUIRED SETTINGS
# ============================================

# Git Repository Configuration
# Your Git repository URL where VM configurations will be stored
GIT_REPO_URL=https://github.com/your-username/your-vm-configs-repo.git

# Your Git username
GIT_USERNAME=your-github-username

# Personal Access Token or Password
# For GitHub: Settings -> Developer settings -> Personal access tokens
# Required scopes: repo (full control)
GIT_TOKEN=your-personal-access-token

# ============================================
# OPTIONAL SETTINGS
# ============================================

# Directory within the Git repository where VM YAML files are stored
# Default: virtualmachines/
YAML_SUBDIRECTORY=virtualmachines/

# Local directory where Git repository will be cloned
# Default: /app/storage/clones (for Docker) or /tmp/kubevirt-portal/clones (for local dev)
GIT_CLONE_DIR=/tmp/kubevirt-portal/clones

# ============================================
# FEATURE FLAGS
# ============================================

# Enable External DNS annotations in Service resources
# Set to 'true' if you have external-dns installed in your cluster
EXTERNAL_DNS_ENABLED=false

# External DNS domain suffix for auto-generating FQDNs
# VM hostname will be: <vm-name>.<EXTERNAL_DNS_DOMAIN>
# Example: test-vm.k8s-lan.alcatrash.net
EXTERNAL_DNS_DOMAIN=k8s-lan.example.com

# Enable MetalLB annotations in Service resources
# Set to 'true' if you have MetalLB installed in your cluster
METALLB_ENABLED=false

# Default MetalLB address pool name
# This will be pre-filled when creating VMs
METALLB_DEFAULT_POOL=default

# Enable viewing VMs running in the Kubernetes cluster
# Set to 'true' if you want to view cluster VMs (requires cluster access)
CLUSTER_VMS_ENABLED=false

# ============================================
# FLASK CONFIGURATION
# ============================================

# Flask secret key for session management
# IMPORTANT: Change this to a random string in production!
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-this-in-production

# Enable CSRF protection (recommended for production)
WTF_CSRF_ENABLED=false

# Enable debug mode (DO NOT enable in production!)
DEBUG=true
